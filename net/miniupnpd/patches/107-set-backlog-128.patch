--- a/miniupnpd.c
+++ b/miniupnpd.c
@@ -399,7 +399,7 @@
 		return -1;
 	}

-	if(listen(s, 5) < 0)
+	if(listen(s, 128) < 0)
 	{
 		syslog(LOG_ERR, "listen(http): %m");
 		close(s);
@@ -679,7 +679,7 @@
 		close(s);
 		s = -1;
 	}
-	else if(listen(s, 5) < 0)
+	else if(listen(s, 128) < 0)
 	{
 		syslog(LOG_ERR, "listen(sctl): %m");
 		close(s);
